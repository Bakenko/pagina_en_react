import React from "react";
import Icon from '@mui/material/Icon';

const ButtonIcon = ({icon}) => {
    const iconStyle ={

    };
    return <Icon style={iconStyle}>{icon}</Icon>
}

export default ButtonIcon;